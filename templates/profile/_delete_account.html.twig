{% extends 'profile/index.html.twig' %}

{% block profile_content %}
	<h3 class="text-danger">{{ 'Supprimer le compte'|trans }}</h3>
	<p>{{ 'Attention, cette action est irréversible. Vous perdrez toutes vos données.'|trans }}</p>

	<form method="post" action="{{ path('app_profile_delete_account') }}">
		<button type="submit" class="btn btn-danger">{{ 'Supprimer mon compte'|trans }}</button>
	</form>
{% endblock %}
