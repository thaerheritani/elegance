{% extends 'profile/index.html.twig' %}

{% block profile_content %}
	<h3 class="text-danger">{{ 'profile.delete_account'|trans }}</h3>
	<p>{{ 'profile.delete_warning'|trans }}</p>

	<form method="post" action="{{ path('app_profile_delete_account') }}">
		<button type="submit" class="btn btn-danger">{{ 'profile.delete_button'|trans }}</button>
	</form>
{% endblock %}
