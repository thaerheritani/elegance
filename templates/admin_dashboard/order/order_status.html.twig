{% extends 'base.html.twig' %}

{% block body %}
	{% include 'shared/nav.html.twig' %}

	<div class="container mt-5">
		<h2>{{ 'order.update_status'|trans }}</h2>

		<div class="card p-4">
			<h5 class="mb-4">{{ 'order.order_number'|trans }}{{ order.id }}</h5>
			<p><strong>{{ 'order.date'|trans }} :</strong> {{ order.orderDate|date('d/m/Y') }}</p>
			<p><strong>{{ 'order.total'|trans }} :</strong> {{ order.total }} €</p>
			<p><strong>{{ 'order.customer'|trans }} :</strong> {{ order.customer.email }}</p>

			{{ form_start(form) }}
			{{ form_row(form.status) }}
			{{ form_row(form.submit) }}
			{{ form_end(form) }}
		</div>
	</div>
{% endblock %}
